# Tooltip 显示更新说明

## 更新时间
2025-11-18

## 更新内容

在K线图的tooltip中新增了**空头组合**的显示，与原有的多头组合并列展示。

## 修改的文件

### frontend/js/app.js

#### 1. 新增全局变量
```javascript
let bearishPatternMap = {}; // 存储空头组合数据，key为日期字符串，value为空头组合
```

#### 2. 数据加载时填充bearishPatternMap
在 `loadVolumeTypes()` 函数中：
```javascript
if (item.bearishPattern) {
    bearishPatternMap[dateStr] = item.bearishPattern;
}
```

#### 3. Tooltip中显示空头组合
在tooltip的formatter函数中，多头组合显示后新增：
```javascript
// 显示空头组合
if (bearishPattern) {
    result += `<span style="color: #ef5350; font-weight: bold;">📉 空头组合:</span><br/>`;
    const patterns = bearishPattern.split(',');
    patterns.forEach(p => {
        const patternLabel = p.trim();
        result += `<span style="color: #ef5350; margin-left: 10px;">• ${patternLabel}</span><br/>`;
    });
}
```

## 显示效果

### 多头组合
- 颜色：绿色 (#26a69a)
- 图标：📈
- 格式：列表显示，支持多个组合

### 空头组合（新增）
- 颜色：红色 (#ef5350)
- 图标：📉
- 格式：列表显示，支持多个组合

## 数据来源

- 数据库表：`daily_chance`
- 字段：
  - `bullish_pattern`：多头组合
  - `bearish_pattern`：空头组合
- API接口：`/api/daily_chance/get_daily_chance`

## 注意事项

1. **仅日K线显示**：空头组合和多头组合仅在日K线周期显示
2. **数据格式**：支持逗号分隔的多个组合，例如："1,2,3"
3. **实时更新**：切换股票时自动加载最新数据
4. **后端兼容**：后端API已经返回bearishPattern字段，无需修改后端代码

## 测试方法

1. 启动后端服务：`python backend/app.py`
2. 打开前端页面：`frontend/index.html`
3. 选择任意股票，切换到日K线
4. 鼠标悬停在K线上，查看tooltip
5. 如果当天有空头组合数据，会在多头组合下方显示

## 日志输出

加载数据时会在控制台输出：
```
每日机会数据加载成功，成交量类型: X 条，赔率总分: X 条，多头组合: X 条，空头组合: X 条，支撑线: X 条，压力线: X 条
```

---

## 开发日期
2025-11-18

