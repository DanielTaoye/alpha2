# R点插件参数配置功能 - 快速验证清单

## ✅ 后端验证

### 1. 运行自动化测试脚本
```bash
cd backend
python scripts/test_r_plugin_config.py
```

**预期结果**: 所有测试通过，输出类似：
```
✅ 临近压力位滞涨距离阈值验证通过: 12.0%
✅ 高位发R涨幅阈值验证通过: 10.0%
✅ 已恢复临近压力位滞涨距离阈值为: 10.0%
✅ 已恢复高位发R涨幅阈值为: 8.0%
测试完成！所有功能正常
```

### 2. 检查配置文件
```bash
cat backend/config/strategy_config.json
```

**预期结果**: 包含 `r_point_plugins` 配置节：
```json
{
  "r_point_plugins": {
    "pressure_stagnation": {
      "distance_threshold_pct": 10.0,
      "description": "..."
    },
    "high_position_r": {
      "gain_threshold_pct": 8.0,
      "description": "..."
    }
  }
}
```

### 3. 测试API接口（需要先启动后端服务）

#### 查询配置
```bash
curl http://localhost:5000/api/config
```

**预期结果**: 返回完整配置，包含新增的R点插件参数

#### 更新配置
```bash
curl -X POST http://localhost:5000/api/config \
  -H "Content-Type: application/json" \
  -d '{
    "pressure_distance_threshold": 12.0,
    "high_position_gain_threshold": 10.0
  }'
```

**预期结果**: 
```json
{
  "code": 200,
  "message": "配置更新成功 (...)",
  "data": { ... }
}
```

## ✅ 前端验证

### 1. 启动服务
```bash
# 启动后端服务（根据你的启动方式）
# 例如: python backend/app.py
```

### 2. 访问配置页面
在浏览器中打开: `http://localhost:5000/config.html`

### 3. 检查页面元素

**应该看到的内容**:
- ✅ "当前配置"区域显示：
  - 临近压力位滞涨-距离阈值: 10.0 %
  - 高位发R-涨幅阈值: 8.0 %

- ✅ "R点插件配置" section包含：
  - 临近压力位滞涨 - 距离阈值（输入框+滑块）
  - 预设按钮：保守型(5%)、较严格(8%)、默认(10%)、宽松型(15%)
  - 高位发R - 涨幅阈值（输入框+滑块）
  - 预设按钮：保守型(15%)、较高(10%)、默认(8%)、灵敏型(5%)

### 4. 测试交互功能

#### 测试1: 拖动滑块
- ✅ 拖动"临近压力位滞涨"滑块
- ✅ 输入框数值同步变化

#### 测试2: 输入框输入
- ✅ 在输入框输入数值（如12）
- ✅ 滑块位置同步变化

#### 测试3: 预设按钮
- ✅ 点击"保守型(5%)"按钮
- ✅ 输入框和滑块都变为5

#### 测试4: 保存配置
- ✅ 修改参数值（如改为12和10）
- ✅ 点击"保存配置"按钮
- ✅ 看到成功提示消息
- ✅ "当前配置"区域更新为新值

#### 测试5: 重新加载
- ✅ 点击"重新加载"按钮
- ✅ 参数恢复为保存的值

## ✅ 功能验证

### 1. 热更新验证
1. 通过Web界面修改参数并保存
2. 不重启服务
3. 再次访问配置页面或调用API
4. **预期**: 新参数已生效

### 2. 默认值验证
1. 手动删除配置文件中的 `r_point_plugins` 节点
2. 重启服务或调用reload API
3. **预期**: 参数使用默认值（10.0和8.0）

### 3. 参数验证
1. 尝试输入负数或超过100的值
2. 点击保存
3. **预期**: 看到错误提示，配置未保存

### 4. R点插件实际使用验证
1. 修改参数（如改为5%和15%）
2. 进行回测或实时分析
3. 检查日志中的参数使用情况
4. **预期**: 日志显示使用新的阈值

示例日志：
```
[临近压力位滞涨-距离检查] ... 距离阈值5.0%
[高位发R] ... 不满足>15.0%条件
```

## 📋 完整验证清单

- [ ] 后端自动化测试通过
- [ ] 配置文件包含新参数
- [ ] API查询接口返回新参数
- [ ] API更新接口正常工作
- [ ] 前端页面正常显示
- [ ] 滑块和输入框同步
- [ ] 预设按钮正常工作
- [ ] 保存配置功能正常
- [ ] 重新加载功能正常
- [ ] 热更新生效
- [ ] 默认值兜底正常
- [ ] 参数验证正常
- [ ] R点插件实际使用新参数

## 🔧 故障排查

### 问题1: 配置保存失败
**检查**:
- 配置文件路径是否正确
- 文件权限是否允许写入
- JSON格式是否正确

### 问题2: 前端显示异常
**检查**:
- 浏览器控制台是否有JavaScript错误
- API接口是否正常响应
- 网络请求状态

### 问题3: 参数未生效
**检查**:
- ConfigService是否正确加载配置
- R点插件服务是否使用ConfigService获取参数
- 日志中是否有相关错误信息

### 问题4: 测试脚本失败
**检查**:
- Python环境是否正确
- 依赖包是否完整
- 工作目录是否在backend目录

## 📞 快速支持

如遇问题，请提供：
1. 错误日志
2. 配置文件内容
3. 浏览器控制台输出
4. 测试脚本运行结果

---

**验证完成后，可以开始在生产环境使用此功能！** 🎉

