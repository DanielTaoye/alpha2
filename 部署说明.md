# 腾讯云服务器部署说明

## 📋 服务器信息

### 服务器配置
- **公网IP**: 124.222.103.102
- **内网IP**: 172.17.48.7
- **操作系统**: Ubuntu Server 24.04 LTS 64位
- **配置**: 2核4GB 1Mbps
- **登录账户**: ubuntu
- **登录密码**: Mty@123456

### 数据库配置
- **内网地址**: 172.17.16.30:3306
- **外网地址**: sh-cdbrg-8f14w39q.sql.tencentcdb.com:25924
- **用户名**: root
- **密码**: MrEPYZus7myr
- **数据库**: stock

### 访问地址
- **前端**: http://124.222.103.102:5000
- **API**: http://124.222.103.102:5000/api/*

---

## 🚀 快速部署步骤

### 1️⃣ 上传项目到服务器

**Windows本地 PowerShell：**
```powershell
scp -r C:\Users\lenovo\Desktop\alpha_strategy_v2 ubuntu@124.222.103.102:/home/ubuntu/
```

**输入密码：** `Mty@123456`

---

### 2️⃣ 登录服务器

```bash
ssh ubuntu@124.222.103.102
# 密码: Mty@123456
```

---

### 3️⃣ 安装依赖环境

```bash
# 更新系统
sudo apt update

# 安装Python3和pip
sudo apt install python3 python3-pip -y

# 安装MySQL客户端（用于测试连接）
sudo apt install mysql-client -y

# 进入项目目录
cd /home/ubuntu/alpha_strategy_v2

# 安装Python依赖
pip3 install -r requirements.txt
```

---

### 4️⃣ 设置环境变量并启动服务

#### 方法1：使用部署脚本（推荐）

```bash
# 添加执行权限
chmod +x deploy.sh stop.sh

# 运行部署脚本
./deploy.sh
```

#### 方法2：手动启动

```bash
# 设置环境变量
export ENV=server

# 后台启动服务
cd backend
nohup python3 app.py > ../logs/app.log 2>&1 &

# 保存进程ID
echo $! > ../app.pid

# 查看日志
tail -f ../logs/app.log
```

---

### 5️⃣ 验证服务

在浏览器访问：
```
http://124.222.103.102:5000
```

或使用 curl 测试：
```bash
curl http://124.222.103.102:5000
```

---

## 🔧 服务管理命令

### 启动服务
```bash
cd /home/ubuntu/alpha_strategy_v2
./deploy.sh
```

### 停止服务
```bash
cd /home/ubuntu/alpha_strategy_v2
./stop.sh
```

### 重启服务
```bash
cd /home/ubuntu/alpha_strategy_v2
./stop.sh
./deploy.sh
```

### 查看日志
```bash
# 查看应用日志
tail -f /home/ubuntu/alpha_strategy_v2/logs/app.log

# 查看最近100行
tail -n 100 /home/ubuntu/alpha_strategy_v2/logs/app.log

# 实时查看所有日志
cd /home/ubuntu/alpha_strategy_v2/logs
tail -f *.log
```

### 查看服务状态
```bash
# 查看进程
ps aux | grep python3

# 查看端口占用
netstat -tlnp | grep 5000

# 或使用 lsof
lsof -i:5000
```

---

## 🔐 防火墙配置

确保服务器安全组允许 5000 端口访问：

### 腾讯云控制台配置
1. 进入云服务器控制台
2. 选择实例 **ins-l2fkypqf**
3. 点击"安全组" → "编辑规则"
4. 添加入站规则：
   - 协议：TCP
   - 端口：5000
   - 来源：0.0.0.0/0（所有来源）
   - 策略：允许

### 服务器防火墙（UFW）
```bash
# 允许5000端口
sudo ufw allow 5000

# 查看防火墙状态
sudo ufw status
```

---

## 📂 项目目录结构

```
/home/ubuntu/alpha_strategy_v2/
├── backend/              # 后端代码
│   └── app.py           # 主程序
├── frontend/            # 前端代码
├── logs/                # 日志文件
│   ├── app.log         # 应用日志
│   ├── api.log         # API日志
│   └── database.log    # 数据库日志
├── config.py           # 配置入口（自动加载环境配置）
├── config_local.py     # 本地环境配置
├── config_server.py    # 服务器环境配置
├── deploy.sh           # 部署脚本
├── stop.sh             # 停止脚本
├── app.pid             # 进程ID文件
└── requirements.txt    # Python依赖
```

---

## 🌍 环境配置说明

### 配置文件切换机制

系统通过环境变量 `ENV` 自动加载不同配置：

- **本地开发**: `ENV=local` (默认) → 使用 `config_local.py`
- **服务器生产**: `ENV=server` → 使用 `config_server.py`

### 配置文件对比

| 配置项 | 本地 (config_local.py) | 服务器 (config_server.py) |
|--------|------------------------|---------------------------|
| 数据库地址 | localhost:3306 | 172.17.16.30:3306 (内网) |
| 数据库密码 | 1234 | MrEPYZus7myr |
| 服务器地址 | 0.0.0.0:5000 | 0.0.0.0:5000 |
| Debug模式 | True | False |

---

## 🔍 常见问题排查

### 问题1：服务启动后无法访问

**排查步骤：**
```bash
# 1. 检查进程是否运行
ps aux | grep python3

# 2. 检查端口是否监听
netstat -tlnp | grep 5000

# 3. 查看日志
tail -f logs/app.log

# 4. 测试本地访问
curl http://127.0.0.1:5000

# 5. 检查防火墙
sudo ufw status
```

---

### 问题2：数据库连接失败

**排查步骤：**
```bash
# 测试数据库连接
mysql -h 172.17.16.30 -P 3306 -u root -p
# 输入密码: MrEPYZus7myr

# 检查配置
python3 -c "from config import DATABASE_CONFIG; print(DATABASE_CONFIG)"
```

---

### 问题3：服务自动停止

**排查步骤：**
```bash
# 查看错误日志
tail -n 100 logs/app.log

# 查看系统日志
sudo journalctl -u python3 -n 100
```

---

## 🔄 更新代码

### 从本地更新到服务器

```bash
# 1. 本地打包（排除不需要的文件）
# 在本地 PowerShell 执行
cd C:\Users\lenovo\Desktop\alpha_strategy_v2
scp -r backend frontend config*.py requirements.txt *.sh ubuntu@124.222.103.102:/home/ubuntu/alpha_strategy_v2/

# 2. 服务器上重启服务
ssh ubuntu@124.222.103.102
cd /home/ubuntu/alpha_strategy_v2
./stop.sh
./deploy.sh
```

---

## 🔐 安全建议

### 1. 修改默认密码
```bash
# 修改ubuntu用户密码
passwd
```

### 2. 配置SSH密钥登录
```bash
# 本地生成密钥（如果没有）
ssh-keygen -t rsa -b 4096

# 上传公钥到服务器
ssh-copy-id ubuntu@124.222.103.102
```

### 3. 禁用root远程登录
```bash
sudo nano /etc/ssh/sshd_config
# 设置: PermitRootLogin no
sudo systemctl restart sshd
```

---

## 📊 性能监控

### 查看系统资源
```bash
# CPU和内存
htop

# 磁盘使用
df -h

# 进程资源
ps aux --sort=-%mem | head
```

### 查看应用性能
```bash
# 请求日志统计
grep "GET\|POST" logs/api.log | wc -l

# 错误统计
grep "ERROR" logs/app.log | wc -l
```

---

## 📞 技术支持

如遇问题，请检查：
1. 日志文件：`logs/app.log`
2. 数据库连接：测试内网地址是否可达
3. 防火墙配置：确保5000端口开放
4. 环境变量：确认 `ENV=server` 已设置

