# 🚀 部署配置方案总结

## 📋 环境配置方案

### 🎯 设计思路

通过**环境变量 `ENV`** 自动切换配置文件，实现本地和服务器使用不同数据库：

```
ENV=local  → config_local.py  → localhost:3306 (本地MySQL)
ENV=server → config_server.py → 172.17.16.30:3306 (腾讯云内网)
```

---

## 📁 新增的配置文件

### 1. `config_local.py` - 本地开发配置
```python
DATABASE_CONFIG = {
    'host': 'localhost',
    'port': 3306,
    'user': 'root',
    'password': '1234',
    'database': 'stock',
    'charset': 'utf8mb4'
}
```

### 2. `config_server.py` - 服务器生产配置
```python
DATABASE_CONFIG = {
    'host': '172.17.16.30',  # 腾讯云内网地址
    'port': 3306,
    'user': 'root',
    'password': 'MrEPYZus7myr',
    'database': 'stock',
    'charset': 'utf8mb4'
}
```

### 3. `config.py` - 配置入口（已修改）
自动根据环境变量加载对应配置：
```python
ENV = os.getenv('ENV', 'local')  # 默认local

if ENV == 'server':
    from config_server import DATABASE_CONFIG, ...
else:
    from config_local import DATABASE_CONFIG, ...
```

---

## 🛠️ 部署工具脚本

| 文件 | 用途 | 使用场景 |
|------|------|----------|
| `deploy.sh` | 服务器部署脚本 | 自动部署和启动服务 |
| `stop.sh` | 停止服务脚本 | 停止运行中的服务 |
| `start_local.bat` | 本地启动脚本 | Windows本地开发 |
| `test_config.py` | 配置测试工具 | 验证配置是否正确 |
| `部署说明.md` | 详细部署文档 | 完整部署指南 |
| `快速部署命令.txt` | 命令清单 | 快速参考 |

---

## 🎮 使用方法

### 本地开发（Windows）

**方法1：使用启动脚本**
```cmd
双击运行: start_local.bat
```

**方法2：手动启动**
```cmd
set ENV=local
cd backend
python app.py
```

### 服务器部署（Ubuntu）

**一键部署：**
```bash
# 1. 上传代码（本地PowerShell）
scp -r C:\Users\lenovo\Desktop\alpha_strategy_v2 ubuntu@124.222.103.102:/home/ubuntu/

# 2. 登录服务器
ssh ubuntu@124.222.103.102

# 3. 运行部署脚本
cd /home/ubuntu/alpha_strategy_v2
chmod +x deploy.sh stop.sh
./deploy.sh
```

**访问地址：**
- 前端：http://124.222.103.102:5000
- API：http://124.222.103.102:5000/api/*

---

## ✅ 验证配置

### 测试配置是否正确

```bash
# 本地测试
set ENV=local
python test_config.py

# 服务器测试
export ENV=server
python3 test_config.py
```

**预期输出：**
```
配置测试工具
当前环境变量: ENV=server
数据库配置:
  主机: 172.17.16.30
  端口: 3306
  ...
✓ 数据库连接成功！
✓ 找到 5566 只股票
```

---

## 🔄 更新流程

### 从本地更新到服务器

```bash
# 1. 本地打包上传（PowerShell）
scp -r backend frontend config*.py requirements.txt *.sh ubuntu@124.222.103.102:/home/ubuntu/alpha_strategy_v2/

# 2. 服务器重启服务
ssh ubuntu@124.222.103.102
cd /home/ubuntu/alpha_strategy_v2
./stop.sh
./deploy.sh
```

---

## 📊 配置对比

| 配置项 | 本地环境 | 服务器环境 |
|--------|----------|------------|
| **ENV变量** | `local` (默认) | `server` |
| **配置文件** | `config_local.py` | `config_server.py` |
| **数据库地址** | `localhost:3306` | `172.17.16.30:3306` |
| **数据库密码** | `1234` | `MrEPYZus7myr` |
| **Debug模式** | `True` | `False` |
| **日志级别** | `DEBUG` | `INFO` |
| **访问地址** | `http://localhost:5000` | `http://124.222.103.102:5000` |

---

## 🔐 安全说明

### .gitignore 已配置

以下敏感文件不会提交到Git：
- ✅ `config_local.py` - 本地配置（含本地密码）
- ✅ `config_server.py` - 服务器配置（含生产密码）
- ✅ `*.log` - 日志文件
- ✅ `logs/` - 日志目录
- ✅ `app.pid` - 进程ID文件
- ✅ `backup_*.sql` - 备份文件

### 密码管理

⚠️ **重要：** 
- 本地密码：`1234` （开发用，可以公开）
- 服务器密码：`MrEPYZus7myr` （生产用，保密）

**config_server.py 不会提交到Git**，确保生产密码安全。

---

## 🆘 常见问题

### Q1: 本地启动连不上数据库？
**A:** 检查环境变量是否设置为 `local`
```cmd
echo %ENV%  # Windows
echo $ENV   # Linux
```

### Q2: 服务器无法访问？
**A:** 检查：
1. 防火墙是否开放5000端口
2. 环境变量是否设置为 `server`
3. 内网数据库地址是否可达

### Q3: 如何切换环境？
**A:** 
```bash
# 切换到本地
set ENV=local     # Windows
export ENV=local  # Linux

# 切换到服务器
set ENV=server     # Windows
export ENV=server  # Linux
```

---

## 📞 技术支持

如有问题，请查看：
1. `部署说明.md` - 详细部署指南
2. `快速部署命令.txt` - 命令清单
3. 运行 `python test_config.py` - 测试配置
4. 查看 `logs/app.log` - 应用日志

